
为什么要使用抽象工厂？

答：工厂方法模式有一个问题就是违背了闭包原则，类的创建依赖工厂类。
	如果想要拓展程序，必须对工厂类进行修改，如何解决？
	就用到抽象工厂模式，创建多个工厂类。
	如果你现在想增加一个Sender实现，则只需做一个实现类实现Sender接口，
	同时做一个工厂类，实现Provider接口，就OK了，无需去改动现成的代码。
	
	<重点就是遵循开闭原则，对扩展开放、对修改关闭>